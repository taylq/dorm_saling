<div class="left-sidebar">
  <h2 style="font-size: 24px;"><%= t "edit" %></h2>
</div>

<%= render "form", product: @product, areas: @areas %>
<br/>
<%= link_to t("back"), products_path,
  class: "btn btn-info" %>
